version: "3.8"

services:
  entelechy:
    build: .
    container_name: entelechy
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml:ro
    restart: unless-stopped
